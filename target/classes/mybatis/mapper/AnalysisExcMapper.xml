<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.ace.dao.AnalysisExcDao">
    <select id="findAll" resultType="com.ace.domain.AnalysisExc">
        SELECT id, pen_name AS penName, group_name AS groupName, colum_name AS columName, pen_gb AS penGb, date
        FROM tb_exc_pen
    </select>
    
    
</mapper>
